#include<stdio.h>
int main()
{
    float celsius = 20;
    float fahrenheit = (celsius * 9 / 5) + 32;
    printf("The temperature 20 degree celsius is equal to %f fahrenheit",fahrenheit);
    return 0;
}